[includeIf "gitdir:~/git/home/**"]
  path = .gitconfig-home

[includeIf "gitdir:~/git/cmd/**"]
  path = .gitconfig-cmd

[includeIf "gitdir:~/git/nab/**"]
  path = .gitconfig-nab

[alias]
	co = checkout
	br = branch
	st = status
	pm = pull origin master

[color]
	ui = auto
